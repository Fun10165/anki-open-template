[project]
name = "anki-open-template"
version = "0.1.0"
description = "Multi-format Anki card template build and packaging toolkit"
readme = "README.md"
license = "GPL-3.0-only"
requires-python = ">=3.13"
dependencies = [
    "genanki==0.13.1",
]
